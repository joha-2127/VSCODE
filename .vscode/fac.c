#include <stdio.h>
int main()
/*
{
    int factorial = 1, n;
    printf("Enter the number:");
    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
    {
        factorial = factorial * i;
    }
    printf("%d", factorial);
    return 0;
}
*/

{
    int first = 0, second = 1, fib, n;
    scanf("%d", &n);
    for (int i = 0; i <= n; i++)
    {
        fib
    }
}